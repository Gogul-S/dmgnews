import {StyleSheet} from 'react-native';
import {DIMENSIONS} from '../../constants/dimensions';

export const styles = StyleSheet.create({
  root: {
    flex: 1,
    paddingBottom: DIMENSIONS.paddingNormal,
  },
  viewSwitcher: {
    marginHorizontal: DIMENSIONS.marginNormal,
  },
});
